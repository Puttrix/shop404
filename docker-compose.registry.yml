services:
  mockshop:
    image: ghcr.io/puttrix/mocksite:latest
    ports:
      - "${PUBLISH_PORT:-8080}:3000"
    environment:
      - PORT=3000
      - GTM_ID=
      - MATOMO_TAG_MANAGER_CONTAINER_URL=
      - OPTIMIZELY_WEB_SNIPPET_URL=
      - ODP_SDK_URL=
